# Pi Camera Service Configuration Example
# Copy this file to .env and customize as needed
# All settings are optional and will use defaults if not specified

# ========== Video Configuration ==========

# Video resolution (64-4096 pixels)
CAMERA_WIDTH=1920
CAMERA_HEIGHT=1080

# Framerate (1-120 fps)
CAMERA_FRAMERATE=30

# H.264 bitrate in bits/second (100000-50000000)
# 8Mbps is a good default for 1080p
CAMERA_BITRATE=8000000

# ========== Streaming Configuration ==========

# MediaMTX RTSP endpoint URL
# Must start with rtsp://
CAMERA_RTSP_URL=rtsp://127.0.0.1:8554/cam

# ========== Camera Control Defaults ==========

# Enable auto white balance on startup (true/false)
CAMERA_ENABLE_AWB=true

# Enable auto exposure on startup (true/false)
CAMERA_DEFAULT_AUTO_EXPOSURE=true

# ========== Camera Hardware Configuration (v2.0) ==========

# Camera sensor model (imx708, imx477, imx296, ov5647, etc.)
# Used for auto-detecting tuning file
CAMERA_CAMERA_MODEL=imx708

# Is this a NoIR (No IR filter) camera module? (true/false)
# NoIR cameras are optimized for night vision/IR illumination
CAMERA_IS_NOIR=false

# Path to libcamera tuning file (optional)
# If not specified, will auto-detect based on camera_model and is_noir
# Pi 5 example: /usr/share/libcamera/ipa/rpi/pisp/imx708_noir.json
# Pi 4 example: /usr/share/libcamera/ipa/rpi/vc4/imx708_noir.json
# CAMERA_TUNING_FILE=/usr/share/libcamera/ipa/rpi/vc4/imx708_noir.json

# ========== API Server Configuration ==========

# API server host (0.0.0.0 for all interfaces)
CAMERA_HOST=0.0.0.0

# API server port (1-65535)
CAMERA_PORT=8000

# ========== Security ==========

# API key for authentication
# If not set or empty, authentication is disabled
# Example: CAMERA_API_KEY=your-secret-key-here
# CAMERA_API_KEY=

# ========== Logging ==========

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
CAMERA_LOG_LEVEL=INFO
